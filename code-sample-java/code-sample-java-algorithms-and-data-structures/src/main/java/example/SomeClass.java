package example;


public class SomeClass {

    public static void main(String[] args) {

        // code
        // paymentsValidator.validate(payment)

        /*
        PaymentValidator:

        1. get all the data you need
        2. create and run a bunch of validators

        var validator = Validator.createValidator()
            .addValidator(new SufficientFundsValidator)
            .addValidator(new SufficientFundsValidator)
            .addValidator(new SufficientFundsValidator)
            .addAsyncValidator(
                listOf(validatro1, validator2)
            )
            .addValidator(new SufficientFundsValidator)
            .addValidator(new SufficientFundsValidator)
         */
    }
}
