/**
 * There are two types of connect implementations:
 * 1. Focused on speed of UNION operation which creates a connect created two nodes (which may be part of another connect)
 * 2. Focused on speed of CONNECTED operation which find if 2 nodes are areConnected
 */
package codesample.algorithms.union;