/**
 * There are two types of union implementations:
 * 1. Focused on speed of UNION operation which creates a union created two nodes (which may be part of another union)
 * 2. Focused on speed of CONNECTED operation which find if 2 nodes are connected
 */
package codesample.algorithms.union;