/**
 * There are two types of union implementations:
 * 1. Focused on speed of 'connect' operation which creates a connect created two nodes (which may be part of another connect)
 * 2. Focused on speed of 'areConnected' operation which finds if 2 nodes are connected
 */
package codesample.algorithms.union;