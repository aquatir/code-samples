spring:
  datasource:
    url: jdbc:tc:postgresql:latest://localhost:5432/postgres
    username: postgres
    password:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true

