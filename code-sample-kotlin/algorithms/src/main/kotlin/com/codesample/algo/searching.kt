package com.codesample.algo

/** Given sorted list, find and index if element */
fun <T: Comparable<T>> binarySearch(list: List<T>, element: T): Int {
    assert(list == list.sorted()) { "list should be sorted for binary search to work" }
    TODO()
}
